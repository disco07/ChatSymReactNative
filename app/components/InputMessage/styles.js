import {StyleSheet} from "react-native";

export default StyleSheet.create({
    input: {
        display: 'flex',
        flexDirection: 'row',
        alignItems: 'center',
        backgroundColor: '#f1f1f2',
        marginRight: 10,
        borderWidth: 1,
        borderRadius: 30
    },
    inputGroup: {
        display: 'flex',
        flexDirection: 'row',
        alignItems: 'center',
        width: '100%',
    }
})